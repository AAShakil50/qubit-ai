{
    Purpose: A straightforward process to thoroughly authorize every request
    Method: JWT Token provided by Firebase
    Steps: 
        1. User Supplies JWT Token with the request
        2. Server verifies the token with Firebase
        3. If the request is authorized, the server processes the request
}