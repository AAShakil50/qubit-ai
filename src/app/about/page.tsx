import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Qubit-AI | About",
    description: "About Qubit, Calendar Scheduling System",
}

export default function About() {
    return (
        <div className="m-16">
            
        </div>
    );
}